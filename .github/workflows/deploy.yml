name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20' # Use uma versão LTS estável
      
      # 💡 Adicione as etapas de instalação e build
      - name: Install Dependencies
        run: npm ci # 'ci' é preferível a 'install' em pipelines de build

      - name: Build Project
        run: npm run build # Garanta que seu 'package.json' tem o script 'build'
      
  # O restante do seu workflow de deploy (ex: peaceiris/actions-gh-pages)
      - name: Deploy to GitHub Pages
        # 💡 Você substitui por este código se estiver usando o método Actions/Pages
        uses: actions/deploy-pages@v4
